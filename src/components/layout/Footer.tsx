import Toolbar from '@mui/material/Toolbar';

const Footer = () => {
	return (
		<Toolbar
			sx={{
				background: '#3b3b3b',
				width: '100%',
				marginTop: 'auto',
			}}
		></Toolbar>
	);
};

export default Footer;
