import Toolbar from '@mui/material/Toolbar';

const Footer = () => {
	return (
		<Toolbar
			sx={{
				bgcolor: 'primary.main',
				width: '100%',
				marginTop: 'auto',
			}}
		></Toolbar>
	);
};

export default Footer;
